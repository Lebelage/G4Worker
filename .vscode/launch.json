{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug Geant4 Project",
            "program": "${workspaceFolder}/build/G4Worker",
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}